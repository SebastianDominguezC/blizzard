[package]
name = "blizzard-engine"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
blizzard-id = { path = "../uid" }
blizzard-engine_derive = { path = "./engine_derive" }
